/*
 * Topic: Tunnel services on an FPC and PIC
 * Seen on:
 *   Junos: mse1_mx304 mse2_mx304
 *   EVO: (none)
 * Pair with: none
 * Variables:
 *   $TS_FPC   e.g. 0
 *   $TS_PIC   e.g. 0
 */
chassis {
    fpc $TS_FPC {
        pic $TS_PIC {
            tunnel-services;
        }
    }
}
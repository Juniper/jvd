/*
 * Topic:   Core aggregate member interface
 * Seen on:
 *   Junos: (none)
 *   EVO:   meg2_acx7509
 * Pair with: none
 * Variables:
 *   $AE_BUNDLE   e.g. ae4
 *   $IFD   e.g. et-1/0/0
 */
interfaces {
    $IFD {
        ether-options {
            802.3ad $AE_BUNDLE;
        }
    }
}
